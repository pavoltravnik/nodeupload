version: '3'
services:
  litecoin:
    build:
      dockerfile: Dockerfile.litecoin.dev
    env_file:
      - ~/.env
    volumes:
      - ~/.litecoin:~/.litecoin
    ports:
      - "9332:9332"
      - "9333:9333"

